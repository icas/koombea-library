<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "KoombeaLibrary" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <%= link_to "KoombeaLibrary", root_url, class: "navbar-brand"%>
        </div>
        <div class="" id="navbar-main">
          <% if current_user %>
            <ul class="nav navbar-nav">
              <li><%= link_to "Books", books_path %></li>
              <li><%= link_to "Editorials", editorials_path %></li>
              <li><%= link_to "Authors", authors_path %></li>
            </ul>
          <% end %>

          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
              <li><%= link_to "Logout", logout_path %></li>
            <% else %>
              <li><%= link_to "Login with Google", "/auth/google_apps" %></li>
            <% end %>
          </ul>

          <%= form_tag books_path, :method => 'get', class: "navbar-form navbar-right" do %>
            <%= text_field_tag :search, params[:search], class: "form-control col-lg-8", placeholder: "Search" %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="container">

      <%= render 'shared/alerts' %>

      <%= yield %>

    </div>

    <div class="container">
      <footer>
        <p>&copy; Koombea-Library 2013</p>
      </footer>
    </div>
  </body>
</html>
